# Dependencies for the packaged Linux release build (see scripts/build_linux.sh).
# Mirrors [tool.poetry.dependencies] in pyproject.toml minus Windows-only
# packages (winotify, pywinstyles, pygrabber, onnxruntime-directml) and dev
# tooling. CPU onnxruntime is used instead of onnxruntime-gpu so the artifact
# runs on every machine — leap.py auto-detects available providers, so users
# who install CUDA get acceleration from source installs, not this bundle.
python-osc>=1.8
requests>=2.28
opencv-python>=4.6
numpy~=2.3.5
pye3d>=0.3.2
sv-ttk>=2.6.1
pydantic>=2.4
scikit-image
scikit-learn>=1.3
pyserial>=3.5
colorama>=0.4.6
matplotlib>=3.10
numba~=0.63.1
onnxruntime>=1.20
openvr>=2.5.101
psutil
pillow

# build tooling
pyinstaller>=6.17
